import React from 'react';  // Import React if you're using React < 17 or if your build setup requires it
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
